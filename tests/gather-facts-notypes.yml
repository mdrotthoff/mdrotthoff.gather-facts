---
- hosts:        localhost
  become:       false
  gather_facts: true
  vars:
  - gather_facts_destination:          'facts/facts-notypes'
  - gather_facts_directory_facts_type: false

  roles:
  - mdrotthoff.gather-facts

