---
# $Author$
# $Date$
# $Source$

# defaults file for mdrotthoff.gather-facts
gather_facts_destination:          'facts'
gather_facts_directory_domain:     true
gather_facts_directory_facts_type: true
gather_facts_host_vars_facts:      true
gather_facts_ansible_vars_facts:   true
gather_facts_ssh_keys_facts:       true
gather_facts_environment_facts:    true
gather_facts_group_facts:          true
#gather_facts_file_owner:           'mdrotthoff'
#gather_facts_file_group:           'ansible'
#gather_facts_file_mode:            '0664'