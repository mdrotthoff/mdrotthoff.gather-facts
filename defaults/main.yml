---
# $Author$
# $Date$
# $Source$

# defaults file for mdrotthoff.gather-facts
gather_facts_ansible_facts:    true
gather_facts_ssh_key_facts:    true
gather_facts_domain_directory: false
gather_facts_destination:      'facts'
gather_facts_host_facts_dir:   "{{ gather_facts_destination | default('facts') }}/host-facts"
gather_facts_ssh_facts_dir:    "{{ gather_facts_destination | default('facts') }}/ssh-keys"
